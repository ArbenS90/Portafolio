---
const projects = [
  {
    title: 'Diseño Web Corporativo',
    description: 'Rediseño completo del sitio web para una empresa de tecnología, enfocado en UX y rendimiento.',
    image: '/images/project-1.jpg',
    tags: ['Diseño Web', 'UI/UX', 'React'],
    link: '#'
  },
  {
    title: 'Aplicación E-commerce',
    description: 'Tienda online con sistema de carrito de compras, pasarela de pagos y panel de administración.',
    image: '/images/project-2.jpg',
    tags: ['E-commerce', 'JavaScript', 'Node.js'],
    link: '#'
  },
  {
    title: 'Dashboard Analytics',
    description: 'Panel de control para visualización de datos con gráficos interactivos y reportes automatizados.',
    image: '/images/project-3.jpg',
    tags: ['Dashboard', 'Data Visualization', 'Vue.js'],
    link: '#'
  },
  {
    title: 'App Móvil',
    description: 'Aplicación móvil para gestión de tareas personales con sincronización en la nube.',
    image: '/images/project-4.jpg',
    tags: ['App Móvil', 'React Native', 'Firebase'],
    link: '#'
  }
];
---

<section id="projects" class="section projects">
  <div class="container">
    <div class="section-header text-center">
      <h2>Mis Proyectos</h2>
      <div class="divider"></div>
      <p>Una selección de trabajos recientes que he realizado.</p>
    </div>
    
    <div class="projects-grid">
      {projects.map(project => (
        <div class="project-card card">
          <div class="project-image">
            <div class="placeholder-project">
              <i class="fas fa-laptop-code"></i>
            </div>
          </div>
          <div class="project-content">
            <h3>{project.title}</h3>
            <p>{project.description}</p>
            <div class="project-tags">
              {project.tags.map(tag => (
                <span class="tag">{tag}</span>
              ))}
            </div>
            <a href={project.link} class="project-link">
              Ver Proyecto <i class="fas fa-arrow-right"></i>
            </a>
          </div>
        </div>
      ))}
    </div>
    
    <div class="text-center mt-lg">
      <a href="#" class="btn">Ver Más Proyectos</a>
    </div>
  </div>
</section>

<style>
  .projects {
    background-color: var(--color-primary);
  }
  
  .section-header p {
    color: var(--color-text-muted);
    max-width: 600px;
    margin: var(--space-md) auto 0;
  }
  
  .projects-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
    gap: var(--space-lg);
  }
  
  .project-card {
    height: 100%;
    display: flex;
    flex-direction: column;
  }
  
  .project-image {
    height: 200px;
    width: 100%;
    background-color: var(--color-primary);
    overflow: hidden;
    position: relative;
  }
  
  .placeholder-project {
    width: 100%;
    height: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
    font-size: 3rem;
    color: rgba(59, 130, 246, 0.1);
    background-color: var(--color-primary-dark);
  }
  
  .project-content {
    padding: var(--space-md);
    flex-grow: 1;
    display: flex;
    flex-direction: column;
  }
  
  .project-content h3 {
    margin-bottom: var(--space-sm);
    font-size: 1.25rem;
  }
  
  .project-content p {
    color: var(--color-text-muted);
    margin-bottom: var(--space-md);
    font-size: 0.95rem;
    flex-grow: 1;
  }
  
  .project-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-bottom: var(--space-md);
  }
  
  .tag {
    padding: 0.25rem 0.5rem;
    background-color: var(--color-primary);
    border: 1px solid var(--color-border);
    border-radius: var(--border-radius);
    font-size: 0.75rem;
    color: var(--color-text-muted);
  }
  
  .project-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    color: var(--color-accent);
    font-weight: 600;
    font-size: 0.95rem;
    transition: var(--transition);
    margin-top: auto;
  }
  
  .project-link:hover {
    gap: 0.75rem;
  }
  
  .mt-lg {
    margin-top: var(--space-lg);
  }
  
  /* Responsive */
  @media (max-width: 576px) {
    .projects-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
