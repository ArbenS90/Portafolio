---
---

<section class="hero">
  <div class="container">
    <div class="hero-content">
      <div class="hero-text animate-fade-in">
        <h1>Hola, soy</h1>
        <div class="name-wrapper">
          <div class="name-container">
            <span class="word">
              <span class="neon-letter">S</span><span class="neon-letter">a</span><span class="neon-letter">n</span><span class="neon-letter">t</span><span class="neon-letter">i</span><span class="neon-letter">a</span><span class="neon-letter">g</span><span class="neon-letter">o</span>
            </span>
            <span class="name-space"></span>
            <span class="word">
              <span class="neon-letter">A</span><span class="neon-letter">r</span><span class="neon-letter">a</span><span class="neon-letter">n</span><span class="neon-letter">g</span><span class="neon-letter">o</span>
            </span>
            <span class="name-space"></span>
            <span class="word">
              <span class="neon-letter">B</span><span class="neon-letter">e</span><span class="neon-letter">n</span><span class="neon-letter">j</span><span class="neon-letter">u</span><span class="neon-letter">m</span><span class="neon-letter">e</span><span class="neon-letter">a</span>
            </span>
          </div>
        </div>
        <h2>Desarrollador Full Stack & Diseñador UI/UX</h2>
        <p>Creo experiencias digitales atractivas y funcionales con un enfoque minimalista y elegante.</p>
        <div class="hero-buttons">
          <a href="#projects" class="btn btn-primary">Ver Proyectos</a>
          <a href="#contact" class="btn btn-primary">Contacto</a>
        </div>
      </div>
    </div>
  </div>
  <div class="scroll-indicator">
    <a href="#about">
      <i class="fas fa-chevron-down"></i>
    </a>
  </div>
</section>

<style>
  :root {
    --neon-color: #3b82f6; /* Color base del neón - debe coincidir con --color-accent */
    --neon-color-bright: #60a5fa; /* Versión más brillante */
    --neon-glow: 0 0 5px rgba(59, 130, 246, 0.8), 0 0 10px rgba(59, 130, 246, 0.5), 0 0 15px rgba(59, 130, 246, 0.3);
    --neon-flicker: 0 0 2px #fff, 0 0 4px #fff, 0 0 8px var(--neon-color), 0 0 12px var(--neon-color), 0 0 16px var(--neon-color);
  }

  .hero {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    position: relative;
  }
  
  .hero-content {
    display: flex;
    align-items: center;
    justify-content: center;
  }
  
  .hero-text {
    max-width: 100%;
    text-align: center;
  }
  
  .hero-text h1 {
    margin-bottom: 0.5rem;
    font-size: 3.5rem;
    line-height: 1.2;
  }
  
  .hero-text h2 {
    color: var(--color-accent);
    font-size: 1.75rem;
    margin-bottom: 1.5rem;
  }
  
  .hero-text p {
    color: var(--color-text-muted);
    font-size: 1.2rem;
    max-width: 700px;
    margin: 0 auto 2rem;
  }
  
  /* Nuevo contenedor para envolver el nombre */
  .name-wrapper {
    width: 100%;
    margin-bottom: 1rem;
    overflow-x: visible;
  }
  
  /* Estilo para el nombre con efecto neón */
  .name-container {
    color: #3b82f6;
    position: relative;
    display: inline-flex;
    white-space: nowrap;
    font-size: 3.5rem;
    font-weight: 700;
    line-height: 1.2;
    align-items: center;
  }
  
  .name-container::after {
    content: '';
    position: absolute;
    width: 100%;
    height: 8px;
    bottom: 5px;
    left: 0;
    background-color: #3b82f6;
    opacity: 0.3;
    z-index: -1;
  }
  
  /* Agrupar palabras */
  .name-container .word {
    display: inline-flex;
    letter-spacing: -0.5px; /* Juntar las letras dentro de cada palabra */
  }
  
  .name-container .name-space {
    display: inline-block;
    width: 18px; /* Espacio entre las palabras */
  }
  
  .neon-letter {
    display: inline-block;
    position: relative;
    transition: all 0.15s ease-in-out;
    color: #3b82f6;
    font-weight: 600;
    padding: 0 1px; /* Reducir el padding entre letras */
    cursor: default;
  }
  
  .neon-letter:hover {
    color: #60a5fa;
    text-shadow: 
      0 0 2px #ffffff,
      0 0 4px #ffffff,
      0 0 8px #3b82f6,
      0 0 12px #3b82f6,
      0 0 16px #3b82f6,
      0 0 22px #3b82f6;
    animation: neonFlicker 1.5s ease-in-out infinite alternate;
    transform: scale(1.05);
  }
  
  @keyframes neonFlicker {
    0%, 19%, 21%, 23%, 25%, 54%, 56%, 100% {
      text-shadow: 
        0 0 2px #ffffff,
        0 0 4px #ffffff,
        0 0 8px #3b82f6,
        0 0 12px #3b82f6,
        0 0 16px #3b82f6,
        0 0 22px #3b82f6;
    }
    20%, 24%, 55% {
      text-shadow: none;
    }
  }
  
  /* Estilos de botones mejorados */
  .hero-buttons {
    display: flex;
    gap: 1.5rem;
    justify-content: center;
    margin-top: 2rem;
  }
  
  /* Selector de mayor especificidad para los botones */
  .hero .hero-buttons .btn {
    position: relative;
    padding: 0.75rem 1.5rem;
    font-weight: 500;
    border-radius: 4px;
    transition: all 0.3s ease;
    font-size: 1rem;
    min-width: 160px;
    text-align: center;
  }
  
  /* Botón principal minimalista */
  .hero .hero-buttons .btn-primary {
    background: linear-gradient(to right, transparent 50%, #3b82f6 50%);
    background-size: 200% 100%;
    background-position: 0 0;
    border: 1px solid #3b82f6;
    color: #ffffff;
    overflow: hidden;
  }
  
  .hero .hero-buttons .btn-primary:hover {
    background-position: -100% 0;
    box-shadow: 0 0 15px rgba(59, 130, 246, 0.4);
  }
  
  /* Botón secundario con alta especificidad */
  .hero .hero-buttons .btn-outline {
    background: linear-gradient(to right, transparent 50%, rgba(59, 130, 246, 0.15) 50%) !important;
    background-size: 200% 100% !important;
    background-position: 0 0 !important;
    border: 1px solid #3b82f6 !important;
    color: #3b82f6 !important;
    overflow: hidden !important;
  }
  
  .hero .hero-buttons .btn-outline:hover {
    background-position: -100% 0 !important;
    box-shadow: 0 0 10px rgba(59, 130, 246, 0.3) !important;
    color: #3b82f6 !important;
    background-color: transparent !important;
  }
  
  .scroll-indicator {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    animation: bounce 2s infinite;
  }
  
  .scroll-indicator a {
    display: flex;
    justify-content: center;
    align-items: center;
    width: 50px;
    height: 50px;
    border-radius: 50%;
    background: linear-gradient(to right, var(--color-primary-light) 50%, #3b82f6 50%);
    background-size: 200% 100%;
    background-position: 0 0;
    color: var(--color-text);
    transition: all 0.3s ease;
    overflow: hidden;
  }
  
  .scroll-indicator a:hover {
    background-position: -100% 0;
    box-shadow: 0 0 15px rgba(59, 130, 246, 0.4);
  }
  
  @keyframes bounce {
    0%, 20%, 50%, 80%, 100% {
      transform: translateY(0) translateX(-50%);
    }
    40% {
      transform: translateY(-20px) translateX(-50%);
    }
    60% {
      transform: translateY(-10px) translateX(-50%);
    }
  }
  
  /* Media queries para responsividad */
  @media (max-width: 1200px) {
    .hero-text h1, .name-container {
      font-size: 3.2rem;
    }
    
    .name-container::after {
      height: 6px;
      bottom: 3px;
    }
  }
  
  @media (max-width: 992px) {
    .hero-text h1, .name-container {
      font-size: 2.8rem;
    }
    
    .hero-text h2 {
      font-size: 1.5rem;
    }
    
    .name-container .name-space {
      width: 15px; /* Espacio reducido entre palabras */
    }
    
    .name-container::after {
      height: 5px;
      bottom: 0;
    }
  }
  
  @media (max-width: 768px) {
    .hero-text h1, .name-container {
      font-size: 2.3rem;
    }
    
    .hero-text h2 {
      font-size: 1.3rem;
    }
    
    .name-container .word {
      letter-spacing: -0.8px; /* Juntar más las letras en dispositivos móviles */
    }
    
    .name-container .name-space {
      width: 12px; /* Espacio más reducido entre palabras */
    }
    
    .name-container::after {
      height: 4px;
      bottom: -2px;
    }
    
    .hero-buttons {
      flex-direction: column;
      gap: 1rem;
      align-items: center;
    }
    
    .hero-buttons .btn {
      width: 80%;
      max-width: 250px;
    }
  }
  
  @media (max-width: 576px) {
    .hero-text h1, .name-container {
      font-size: 1.8rem;
    }
    
    .hero-text h2 {
      font-size: 1.1rem;
    }
    
    .hero-text p {
      font-size: 1rem;
    }
    
    .name-wrapper {
      margin-bottom: 0.5rem;
    }
    
    .name-container::after {
      height: 3px;
      bottom: -4px;
    }
    
    /* En móviles muy pequeños, mostrar el nombre en varias líneas */
    @media (max-width: 360px) {
      .name-container {
        display: flex;
        flex-direction: column;
        align-items: center;
        line-height: 1.3;
      }
      
      .name-container .name-space {
        display: none; /* Quitar espacios explícitos ya que usamos saltos de línea */
      }
      
      .name-container::after {
        display: none; /* Quitar el subrayado en vista móvil muy pequeña */
      }
    }
  }
</style>
